org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
# zxing-android-embedded 3.6.0 ships the legacy android support library; Jetifier
# rewrites it to AndroidX at build time so it coexists with the app's androidx deps.
android.enableJetifier=true
kotlin.code.style=official
org.gradle.caching=true
