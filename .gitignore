build/
host/build/
compile_commands.json
complete builds/
web/node_modules/
# web/dist is committed so Flatpak/AppImage packaging can consume it without Node on the builder
# (rebuild with `cd web && npm ci && npm run build` when sources change)

# compiled spike binaries (only the sources are tracked)
spike/**/aoa_spike

# personal scratch notes
im lazy

# spacedesk reverse-engineering run artifacts: captures, decoded stripes and mitm logs are
# produced per session and are large/uninteresting — only the tools themselves are tracked.
__pycache__/
tools/spacedesk-re/*.bin
tools/spacedesk-re/*.jpg
tools/spacedesk-re/*.log
tools/spacedesk-re/*.stdout
